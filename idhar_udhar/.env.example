# Example environment file — copy to `.env` locally.
# NEVER put real credentials in this file. NEVER commit `.env`.

APP_ENV=development
API_BASE_URL=YOUR_API_BASE_URL
API_KEY=YOUR_API_KEY
SECRET_KEY=YOUR_SECRET_KEY
