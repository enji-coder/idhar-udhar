/**
 * Quote body is empty on purpose.
 * Client-supplied distance / duration / fare are rejected (forbidNonWhitelisted).
 * Routed distance comes from RoutingService using stored order stops.
 */
export class QuoteOrderDto {}
